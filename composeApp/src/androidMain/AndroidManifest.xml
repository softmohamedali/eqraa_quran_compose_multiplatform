<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".App"
        android:icon="@android:drawable/ic_menu_compass"
        android:label="Eqraa"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        >
        <activity
            android:name=".presentation.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".core.reiver.NotificationActionBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="START" />
                <action android:name="STOP" />
                <action android:name="PLAY" />
                <action android:name="PAUSE" />
                <action android:name="SKIP10" />
                <action android:name="BACK10" />
            </intent-filter>
        </receiver>

        <service
            android:name=".core.services.AudioServices"
            android:foregroundServiceType="mediaPlayback"
            android:enabled="true"/>

        <receiver android:name=".presentation.screens.sebha.SimpleCounterWidgetReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sebha_widget_info" />
        </receiver>
    </application>

</manifest>